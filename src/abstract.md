# 추상클래스

# 순수 추상 클래스
  ╰모든 메서드가 추상 메서드인 추상 클래스, 실행 로직을 가지고 있지않고 단지 다형성을 위한 부모 타입으로써 껍데기 역할만 제공한다.
  
 1. 인스턴스를 생성할 수 없다.
 2. 상속시 자식은 모든 메서드를 오버라이딩 해야 한다.
 3. 주로 다형성을 위해 사용된다.

이 순수 추상 클래스(USB 인터페이스 규격)를 더 편리하게 사용할수 있도록 하는 것이 `인터페이스`이다.